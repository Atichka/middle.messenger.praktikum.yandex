ul Ссылки на страницы
    li
        button.loginLink(type='submit', onclick="renderTemplate('login')") login
    li
        button.signinLink(type='submit', onclick="renderTemplate('signin')") signin
    li
        button.chatsLink(type='submit', onclick="renderTemplate('chats')") chats
    li
        button.profileLink(type='submit', onclick="renderTemplate('profile')") profile
    li
        button.profileEditLink(type='submit', onclick="renderTemplate('profileEdit')") profile edit
    li
        button.passwordEditLink(type='submit', onclick="renderTemplate('passwordEdit')") password edit
    li
        a(href='./pages/error404.pug') error 404
    li
        a(href='./pages/error500.pug') error 500
